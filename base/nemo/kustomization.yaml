apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Part 0 — in-cluster NeMo Guardrails (CPU-only; delegates inference to remote MaaS models).
resources:
  - namespace.yaml
  - nemo-config.configmap.yaml
  - nemoguardrails.yaml
  - service.yaml
  - route.yaml
  - networkpolicy.yaml
