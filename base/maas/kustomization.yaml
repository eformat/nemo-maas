apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Part A — NeMo chat proxy published as a MaaS model.
resources:
  - externalmodel.yaml
  - credential.secret.yaml
  - maasmodelref.yaml
  - maassubscription.yaml
  - maasauthpolicy.yaml
